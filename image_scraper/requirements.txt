# Image-collection subsystem dependencies (all free / open-source).
# Install:  pip install -r requirements.txt
# Then, for JavaScript-heavy sites:  playwright install chromium

httpx>=0.27          # polite static page + image fetching
trafilatura>=1.9     # article text / metadata extraction
beautifulsoup4>=4.12 # HTML parsing for images + captions
Pillow>=10.1         # contact-sheet compositing
playwright>=1.44     # headless-browser fallback for JS-rendered pages
